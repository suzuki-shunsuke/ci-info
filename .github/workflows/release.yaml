---
name: Release
on:
  push:
    tags: [v*]
permissions: {}
jobs:
  release:
    uses: suzuki-shunsuke/go-release-workflow/.github/workflows/release.yaml@feat-cosign-v3
    with:
      go-version-file: go.mod
      aqua_version: v2.55.0
    permissions:
      contents: write
      id-token: write
      actions: read
      attestations: write
